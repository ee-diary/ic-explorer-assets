// 50px filter padding fix to prevent vertical shift when pins are clicked
const filterPadding = 50; // Adjusted padding value
const filterWidth = originalFilterWidth + filterPadding; // Adjusted filter dimensions
const filterHeight = originalFilterHeight + filterPadding; // Adjusted filter dimensions
// ... rest of the code ...